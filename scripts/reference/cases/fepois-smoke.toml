schema_version = 1
id = "fepois-smoke"
estimator = "fepois"
formula = "Y ~ X1 + X2 | f1"
prediction_rows = 5

# Poisson IRLS and recovered fixed effects differ slightly across
# implementations even when coefficient estimates agree more closely.
rtol = 1e-4
atol = 1e-5

[data]
source = "generated"
seed = 7651
n = 500
model = "Fepois"

[vcov]
type = "iid"

[ssc]
k_adj = true
k_fixef = "nonnested"
g_adj = true
g_df = "min"
t_df = "min"
